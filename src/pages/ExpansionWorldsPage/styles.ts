import styled from 'styled-components'

export const Container = styled.div`
  padding-left: 86px;
  position: absolute;
  top: 50%;
  left: 50%;
  transform: translate(-50%, -45%);

  display: flex;
`
